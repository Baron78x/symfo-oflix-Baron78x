{% extends 'back/base_back.html.twig' %}

{% block title %}Modifier un film{% endblock %}

{% block body_back %}
    {% if movie.type == "Film" %} 
    <h1>Modifier le film {{ movie.title }}</h1>
    {% elseif movie.type == "Série" %}
    <h1>Modifier la série {{ movie.title }}</h1>
    {% endif %}

    {{ include('back/movie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_movie_index') }}" class="btn btn-primary mb-2">Retour à la liste</a>

    {{ include('back/movie/_delete_form.html.twig') }}
{% endblock %}
